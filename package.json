{
  "name": "@tscircuit/create-snippet-url",
  "main": "dist/index.js",
  "version": "0.0.8",
  "type": "module",
  "scripts": {
    "build": "tsup-node lib/index.ts --dts --format esm",
    "format": "biome format --write .",
    "format:check": "biome format ."
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest",
    "tsup": "^8.3.5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
